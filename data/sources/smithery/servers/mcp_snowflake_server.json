{
  "qualifiedName": "mcp_snowflake_server",
  "displayName": "Snowflake Server",
  "description": "A Model Context Protocol server implementation that provides database interaction with Snowflake, allowing SQL queries and data insights.",
  "iconUrl": null,
  "remote": false,
  "deploymentUrl": null,
  "connections": [
    {
      "type": "stdio",
      "stdioFunction": "config => ({command: 'uvx', args: ['mcp_snowflake_server'], env: {SNOWFLAKE_WAREHOUSE: config.snowflakeWarehouse, SNOWFLAKE_DATABASE: config.snowflakeDatabase, SNOWFLAKE_ACCOUNT: config.snowflakeAccount, SNOWFLAKE_USER: config.snowflakeUser, SNOWFLAKE_ROLE: config.snowflakeRole, SNOWFLAKE_SCHEMA: config.snowflakeSchema, SNOWFLAKE_PASSWORD: config.snowflakePassword}})",
      "configSchema": {
        "type": "object",
        "required": [
          "snowflakeWarehouse",
          "snowflakeDatabase",
          "snowflakeAccount",
          "snowflakeUser",
          "snowflakeRole",
          "snowflakeSchema",
          "snowflakePassword"
        ],
        "properties": {
          "snowflakeRole": {
            "type": "string",
            "description": "The Snowflake role."
          },
          "snowflakeUser": {
            "type": "string",
            "description": "The Snowflake username."
          },
          "snowflakeSchema": {
            "type": "string",
            "description": "The Snowflake schema."
          },
          "snowflakeAccount": {
            "type": "string",
            "description": "The Snowflake account identifier."
          },
          "snowflakeDatabase": {
            "type": "string",
            "description": "The Snowflake database to use."
          },
          "snowflakePassword": {
            "type": "string",
            "description": "The Snowflake password."
          },
          "snowflakeWarehouse": {
            "type": "string",
            "description": "The Snowflake warehouse to connect to."
          }
        }
      }
    }
  ],
  "security": null,
  "tools": null,
  "_sync": {
    "fetched_at": "2026-01-04T15:49:27.635827+00:00",
    "source": "smithery"
  }
}