{
  "qualifiedName": "@weidwonder/terminal-mcp-server",
  "displayName": "Terminal MCP Server",
  "description": "Execute commands on local or remote hosts seamlessly. Leverage a powerful interface to run system commands, manage sessions, and set environment variables effortlessly. Enhance your AI models and applications with robust command execution capabilities.",
  "iconUrl": null,
  "remote": false,
  "deploymentUrl": null,
  "connections": [
    {
      "type": "stdio",
      "bundleUrl": "https://backend.smithery.ai/storage/v1/object/public/bundles/@weidwonder/terminal-mcp-server/server.mcpb",
      "runtime": "node",
      "configSchema": {
        "type": "object",
        "required": [],
        "properties": {
          "sse": {
            "type": "boolean",
            "default": false,
            "description": "If true, run the server in SSE mode instead of default stdio mode."
          },
          "host": {
            "type": "string",
            "default": "localhost",
            "description": "Host to bind to when running in SSE mode."
          },
          "port": {
            "type": "number",
            "default": 8080,
            "description": "Port to listen on when running in SSE mode."
          },
          "endpoint": {
            "type": "string",
            "default": "/sse",
            "description": "Endpoint path for SSE connections."
          }
        }
      }
    }
  ],
  "security": {
    "scanPassed": true
  },
  "tools": [
    {
      "name": "execute_command",
      "description": "Execute commands on remote hosts or locally (This tool can be used for both remote hosts and the current machine)",
      "inputSchema": {
        "type": "object",
        "required": [
          "command"
        ],
        "properties": {
          "env": {
            "type": "object",
            "default": {},
            "description": "Environment variables"
          },
          "host": {
            "type": "string",
            "description": "Host to connect to (optional, if not provided the command will be executed locally)"
          },
          "command": {
            "type": "string",
            "description": "Command to execute. Before running commands, it's best to determine the system type (Mac, Linux, etc.)"
          },
          "session": {
            "type": "string",
            "default": "default",
            "description": "Session name, defaults to 'default'. The same session name will reuse the same terminal environment for 20 minutes, which is useful for operations requiring specific environments like conda."
          },
          "username": {
            "type": "string",
            "description": "Username for SSH connection (required when host is specified)"
          }
        }
      }
    }
  ],
  "_sync": {
    "fetched_at": "2026-01-04T13:09:30.854453+00:00",
    "source": "smithery"
  }
}