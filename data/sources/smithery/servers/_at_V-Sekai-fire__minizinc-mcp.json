{
  "qualifiedName": "@V-Sekai-fire/minizinc-mcp",
  "displayName": "V-Sekai-fire's Minizinc",
  "description": "Solve constraint programming problems with MiniZinc.",
  "iconUrl": "https://icons.duckduckgo.com/ip3/github.com.ico",
  "remote": true,
  "deploymentUrl": "https://server.smithery.ai/@V-Sekai-fire/minizinc-mcp",
  "connections": [
    {
      "type": "http",
      "deploymentUrl": "https://server.smithery.ai/@V-Sekai-fire/minizinc-mcp/mcp",
      "configSchema": {
        "description": "Schema for the /mcp endpoint configuration"
      }
    }
  ],
  "security": null,
  "tools": [
    {
      "name": "minizinc_solve",
      "title": "Solve MiniZinc Model",
      "annotations": {
        "readOnlyHint": true,
        "idempotentHint": false,
        "destructiveHint": false
      },
      "description": "Solves a MiniZinc model using chuffed solver (fixed, not configurable).\n\nStandard libraries: By default, automatically includes common MiniZinc standard libraries (e.g., alldifferent.mzn) \nif not already present in the model. This can be controlled via the auto_include_stdlib parameter (default: true).\nThis allows models to use standard functions without explicit includes.\n\nOutput format:\n- DZN format: Variables are parsed from DZN format when available (models without explicit output statements)\n- Output text: Explicit output statements are passthrough'd in output_text field\n- Both formats are included when available\n",
      "inputSchema": {
        "type": "object",
        "properties": {
          "timeout": {
            "type": "integer",
            "description": "Optional timeout in milliseconds (default: 30000, i.e., 30 seconds). Maximum allowed is 30000 ms (30 seconds); values exceeding this will be capped at 30 seconds."
          },
          "data_content": {
            "type": "string",
            "description": "Optional .dzn data content as string. Must be valid DZN format (e.g., 'n = 8;'). Parsed and included in response."
          },
          "model_content": {
            "type": "string",
            "description": "MiniZinc model content (.mzn) as string"
          },
          "auto_include_stdlib": {
            "type": "boolean",
            "default": true,
            "description": "Automatically include standard MiniZinc libraries (e.g., alldifferent.mzn) if not present (default: true)"
          }
        }
      }
    },
    {
      "name": "minizinc_validate",
      "title": "Validate MiniZinc Model",
      "annotations": {
        "readOnlyHint": true,
        "idempotentHint": true,
        "destructiveHint": false
      },
      "description": "Validates a MiniZinc model by checking syntax and type checking without solving.\nUseful for debugging models before attempting to solve them.\n\nReturns detailed error and warning messages if the model is invalid.\n",
      "inputSchema": {
        "type": "object",
        "properties": {
          "data_content": {
            "type": "string",
            "description": "Optional .dzn data content as string. Must be valid DZN format (e.g., 'n = 8;')."
          },
          "model_content": {
            "type": "string",
            "description": "MiniZinc model content (.mzn) as string"
          },
          "auto_include_stdlib": {
            "type": "boolean",
            "default": true,
            "description": "Automatically include standard MiniZinc libraries (e.g., alldifferent.mzn) if not present (default: true)"
          }
        }
      }
    }
  ],
  "_sync": {
    "fetched_at": "2026-01-04T13:51:07.363032+00:00",
    "source": "smithery"
  }
}