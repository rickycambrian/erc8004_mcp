{
  "id": "@bekirdag/docdex",
  "name": "@bekirdag/docdex",
  "display_name": "docdex",
  "version": null,
  "description": "Index and search your project\u2019s docs locally to surface precise, copy-ready snippets. Keep context fresh by reindexing changed files on demand. Open files, list indexed docs, and pull summaries to ground your work in authoritative sources.",
  "icon_url": "https://spjawbfpwezjfmicopsl.supabase.co/storage/v1/object/public/server-icons/de2b89e9-2e14-4220-a38a-1da966df8b26.png",
  "repository_url": null,
  "status": "active",
  "published_at": null,
  "tools": [
    {
      "name": "docdex_search",
      "annotations": {},
      "description": "Search repository docs and return hits with rel_path, summary, snippet, and doc_id.",
      "inputSchema": {
        "type": "object",
        "required": [
          "query"
        ],
        "properties": {
          "limit": {
            "type": "integer",
            "default": 8,
            "maximum": 8,
            "minimum": 1,
            "description": "Max results to return (clamped to server max)"
          },
          "query": {
            "type": "string",
            "minLength": 1,
            "description": "Concise search query (will be rejected if empty)"
          },
          "project_root": {
            "type": "string",
            "description": "Optional repo root; must match the MCP server repo"
          }
        }
      }
    },
    {
      "name": "docdex_index",
      "annotations": {},
      "description": "Rebuild the index (or ingest specific files) for the current repo root.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Optional list of files to ingest; empty => full reindex"
          },
          "project_root": {
            "type": "string",
            "description": "Optional repo root; must match the MCP server repo"
          }
        }
      }
    },
    {
      "name": "docdex_files",
      "annotations": {},
      "description": "List indexed documents (rel_path/doc_id/token_estimate) for the current repo.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "default": 200,
            "maximum": 1000,
            "minimum": 1,
            "description": "Max documents to return (clamped)"
          },
          "offset": {
            "type": "integer",
            "default": 0,
            "maximum": 50000,
            "minimum": 0,
            "description": "Number of docs to skip before listing (clamped)"
          },
          "project_root": {
            "type": "string",
            "description": "Optional repo root; must match the MCP server repo"
          }
        }
      }
    },
    {
      "name": "docdex_open",
      "annotations": {},
      "description": "Read a file from the repo (optional line window); rejects paths outside the repo.",
      "inputSchema": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Relative path under the repo"
          },
          "end_line": {
            "type": "integer",
            "minimum": 1,
            "description": "Optional end line (1-based, inclusive)"
          },
          "start_line": {
            "type": "integer",
            "minimum": 1,
            "description": "Optional start line (1-based, inclusive)"
          },
          "project_root": {
            "type": "string",
            "description": "Optional repo root; must match the MCP server repo"
          }
        }
      }
    },
    {
      "name": "docdex_stats",
      "annotations": {},
      "description": "Inspect index metadata: doc count, state dir, size on disk, and last update time.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "project_root": {
            "type": "string",
            "description": "Optional repo root; must match the MCP server repo"
          }
        }
      }
    }
  ],
  "tool_count": 5,
  "prompts": [],
  "resources": [],
  "remote_endpoint": "https://server.smithery.ai/@bekirdag/docdex/mcp",
  "packages": [],
  "sources": [
    "smithery"
  ],
  "primary_source": "smithery",
  "is_latest": true,
  "verified": false,
  "use_count": 0,
  "has_remote": true,
  "has_tools": true,
  "tool_names": [
    "docdex_search",
    "docdex_index",
    "docdex_files",
    "docdex_open",
    "docdex_stats"
  ]
}