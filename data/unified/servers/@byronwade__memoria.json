{
  "id": "@byronwade/memoria",
  "name": "@byronwade/memoria",
  "display_name": "memoria",
  "version": null,
  "description": "Prevents your AI from breaking code by revealing hidden file dependencies through git forensics.",
  "icon_url": "https://icons.duckduckgo.com/ip3/github.com.ico",
  "repository_url": null,
  "status": "active",
  "published_at": null,
  "tools": [
    {
      "name": "analyze_file",
      "annotations": {
        "title": "Analyze File",
        "readOnlyHint": true,
        "openWorldHint": false,
        "idempotentHint": true
      },
      "description": "Returns forensic history, hidden dependencies, and risk assessment. USE THIS before modifying files.",
      "inputSchema": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "path": {
            "type": "string",
            "description": "The ABSOLUTE path to the file (e.g. C:/dev/project/src/file.ts)"
          }
        }
      }
    },
    {
      "name": "ask_history",
      "annotations": {
        "title": "Search Git History",
        "readOnlyHint": true,
        "openWorldHint": false,
        "idempotentHint": true
      },
      "description": "Search git history for WHY code was written. IMPORTANT: Use SHORT, SPECIFIC KEYWORDS (e.g., 'serialization', 'race condition', 'timeout'). Do NOT use full sentences or long descriptions - they will return 0 results. Use when asking 'why does this exist?' or before removing code.",
      "inputSchema": {
        "type": "object",
        "required": [
          "query"
        ],
        "properties": {
          "path": {
            "type": "string",
            "description": "Optional: ABSOLUTE path to scope search to a specific file or directory"
          },
          "limit": {
            "type": "number",
            "description": "Maximum number of results to return (default: 20)"
          },
          "query": {
            "type": "string",
            "description": "A single keyword or short phrase (1-3 words max). Examples: 'serialization', 'race condition', 'Safari fix'. Do NOT use full sentences - git grep requires exact matches."
          },
          "endLine": {
            "type": "number",
            "description": "End line for line-range search (requires path). Used with startLine to trace history of a code block."
          },
          "startLine": {
            "type": "number",
            "description": "Start line for line-range search (requires path). Shows history of specific lines using git log -L."
          },
          "searchType": {
            "enum": [
              "message",
              "diff",
              "both"
            ],
            "type": "string",
            "description": "Where to search: 'message' (commit messages only), 'diff' (code changes only), 'both' (default)"
          }
        }
      }
    }
  ],
  "tool_count": 2,
  "prompts": [],
  "resources": [],
  "remote_endpoint": "https://server.smithery.ai/@byronwade/memoria/mcp",
  "packages": [],
  "sources": [
    "smithery"
  ],
  "primary_source": "smithery",
  "is_latest": true,
  "verified": false,
  "use_count": 0,
  "has_remote": true,
  "has_tools": true,
  "tool_names": [
    "analyze_file",
    "ask_history"
  ]
}