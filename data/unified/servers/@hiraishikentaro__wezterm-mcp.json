{
  "id": "@hiraishikentaro/wezterm-mcp",
  "name": "@hiraishikentaro/wezterm-mcp",
  "display_name": "WezTerm MCP Server",
  "version": null,
  "description": "Enable integration of WezTerm terminal emulator with external tools and resources through the Model Context Protocol. Enhance your terminal experience by allowing dynamic access to data and actions via MCP. Simplify automation and context-aware workflows within WezTerm.",
  "icon_url": "https://icons.duckduckgo.com/ip3/github.com.ico",
  "repository_url": null,
  "status": "active",
  "published_at": null,
  "tools": [
    {
      "name": "write_to_terminal",
      "description": "Writes text to the active WezTerm pane - often used to run commands",
      "inputSchema": {
        "type": "object",
        "required": [
          "command"
        ],
        "properties": {
          "command": {
            "type": "string",
            "description": "The command to run or text to write to the terminal"
          }
        }
      }
    },
    {
      "name": "read_terminal_output",
      "description": "Reads output from the active WezTerm pane",
      "inputSchema": {
        "type": "object",
        "properties": {
          "lines": {
            "type": "number",
            "description": "Number of lines to read from the terminal (default: 50)"
          }
        }
      }
    },
    {
      "name": "send_control_character",
      "description": "Sends control characters to the active WezTerm pane",
      "inputSchema": {
        "type": "object",
        "required": [
          "character"
        ],
        "properties": {
          "character": {
            "type": "string",
            "description": "Control character to send (e.g., 'c' for Ctrl+C)"
          }
        }
      }
    },
    {
      "name": "list_panes",
      "description": "Lists all panes in the current WezTerm window",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "switch_pane",
      "description": "Switches to a specific pane in WezTerm",
      "inputSchema": {
        "type": "object",
        "required": [
          "pane_id"
        ],
        "properties": {
          "pane_id": {
            "type": "number",
            "description": "ID of the pane to switch to"
          }
        }
      }
    },
    {
      "name": "write_to_specific_pane",
      "description": "Writes text to a specific WezTerm pane by pane ID",
      "inputSchema": {
        "type": "object",
        "required": [
          "command",
          "pane_id"
        ],
        "properties": {
          "command": {
            "type": "string",
            "description": "The command to run or text to write to the terminal"
          },
          "pane_id": {
            "type": "number",
            "description": "ID of the pane to write to"
          }
        }
      }
    }
  ],
  "tool_count": 6,
  "prompts": [],
  "resources": [],
  "remote_endpoint": null,
  "packages": [],
  "sources": [
    "smithery"
  ],
  "primary_source": "smithery",
  "is_latest": true,
  "verified": false,
  "use_count": 0,
  "has_remote": false,
  "has_tools": true,
  "tool_names": [
    "write_to_terminal",
    "read_terminal_output",
    "send_control_character",
    "list_panes",
    "switch_pane",
    "write_to_specific_pane"
  ]
}