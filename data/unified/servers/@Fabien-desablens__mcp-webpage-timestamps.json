{
  "id": "@Fabien-desablens/mcp-webpage-timestamps",
  "name": "@Fabien-desablens/mcp-webpage-timestamps",
  "display_name": "Webpage Timestamps Extractor",
  "version": null,
  "description": "Extract creation, modification, and publication timestamps from webpages to evaluate content freshness and support temporal analysis. Utilize multiple data sources including HTML meta tags, HTTP headers, JSON-LD, and heuristic methods for comprehensive timestamp detection. Process single or batch URLs with configurable options and confidence scoring for reliable results.",
  "icon_url": null,
  "repository_url": null,
  "status": "active",
  "published_at": null,
  "tools": [
    {
      "name": "extract_timestamps",
      "description": "Extract creation, modification, and publication timestamps from a webpage",
      "inputSchema": {
        "type": "object",
        "required": [
          "url"
        ],
        "properties": {
          "url": {
            "type": "string",
            "description": "The URL of the webpage to extract timestamps from"
          },
          "config": {
            "type": "object",
            "properties": {
              "timeout": {
                "type": "number",
                "description": "Request timeout in milliseconds (default: 10000)"
              },
              "userAgent": {
                "type": "string",
                "description": "User agent string to use for requests"
              },
              "maxRedirects": {
                "type": "number",
                "description": "Maximum number of redirects to follow (default: 5)"
              },
              "followRedirects": {
                "type": "boolean",
                "description": "Whether to follow HTTP redirects (default: true)"
              },
              "enableHeuristics": {
                "type": "boolean",
                "description": "Whether to enable heuristic timestamp detection (default: true)"
              }
            },
            "description": "Optional configuration for the extraction"
          }
        }
      }
    },
    {
      "name": "batch_extract_timestamps",
      "description": "Extract timestamps from multiple webpages in batch",
      "inputSchema": {
        "type": "object",
        "required": [
          "urls"
        ],
        "properties": {
          "urls": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Array of URLs to extract timestamps from"
          },
          "config": {
            "type": "object",
            "properties": {
              "timeout": {
                "type": "number",
                "description": "Request timeout in milliseconds (default: 10000)"
              },
              "userAgent": {
                "type": "string",
                "description": "User agent string to use for requests"
              },
              "maxRedirects": {
                "type": "number",
                "description": "Maximum number of redirects to follow (default: 5)"
              },
              "followRedirects": {
                "type": "boolean",
                "description": "Whether to follow HTTP redirects (default: true)"
              },
              "enableHeuristics": {
                "type": "boolean",
                "description": "Whether to enable heuristic timestamp detection (default: true)"
              }
            },
            "description": "Optional configuration for the extraction"
          }
        }
      }
    }
  ],
  "tool_count": 2,
  "prompts": [],
  "resources": [],
  "remote_endpoint": null,
  "packages": [],
  "sources": [
    "smithery"
  ],
  "primary_source": "smithery",
  "is_latest": true,
  "verified": false,
  "use_count": 0,
  "has_remote": false,
  "has_tools": true,
  "tool_names": [
    "extract_timestamps",
    "batch_extract_timestamps"
  ]
}